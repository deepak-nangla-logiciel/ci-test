const path = require("path");
module.exports = {
  platformName: "iOS",
  "appium:options": {
    deviceName: "iPad Pro 13-inch (M4)",
    platformVersion: "17.4",
    automationName: "XCUITest",
    app: path.resolve(__dirname, "../apps/Leap.app"), //Create a folder named apps in the config folder and place the app there
    autoAcceptAlerts: false,
    noReset: true, // Ensures the app is not reinstalled
    fullReset: false, // Avoids wiping data between tests
    newCommandTimeout: 300,
  },
};
