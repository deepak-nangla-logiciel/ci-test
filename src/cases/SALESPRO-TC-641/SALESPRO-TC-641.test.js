

describe("SALESPRO-TC-641", () => {
  it("Verify that overwrite price UpCharge changes value only for the current UC when UC set as a whole number", async () => {
    expect(true).toBe(true)
  });
});